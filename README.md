Code for testing out new ways of visualizing GPS tracks for orienteering.

Install dependencies
--------------------

	```
        conda config --set channel_priority strict
	conda create --name gps-analysis --file environment.yml
        ```



